# 

- Roll Trait: can return a random number
- ?RollRng Trait: can return a random number; requires rng to be passed in
- ?BakedRng Struct: has a RollRng object and a rand object. Itself implements Roll trait.
